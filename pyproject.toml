[project]
name = "challenge-01-licence-application"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40",
    "flask>=3.1",
    "notifications-python-client>=9.0.0",
]

[tool.uv]
dev-dependencies = []
