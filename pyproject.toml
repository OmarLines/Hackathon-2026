[project]
name = "challenge-01-licence-application"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1",
]

[tool.uv]
dev-dependencies = [
    "pytest>=9.0.3",
    "pytest-flask>=1.3.0",
    "ruff>=0.15.10",
]
